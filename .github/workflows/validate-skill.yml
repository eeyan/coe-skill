name: Validate COE Skill

on:
  push:
    paths: ['.claude/skills/coe/**']
  pull_request:
    paths: ['.claude/skills/coe/**']

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run skill validation
        run: bash tests/validate-skill.sh
